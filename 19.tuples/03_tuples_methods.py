# there is only some number of methods are present in the touples because there are immutable 
# so they can't be changed
tu=(1,5,65,3,1,34,1,3)

print(tu.count(1)) # it will shows how many times did 1 repeated in the tuple
print(tu.index(5)) #it will represent the indexing of the values present in the tuples