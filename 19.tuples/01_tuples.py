# tuples are also one of the 4 built-in data types present in python programming language
# tupels are similar to lists but there are represented uaing ()
# tuples are ordered and there are immutable
tuble=(1,3,5,6)
print(tuble)
tuble2=(4, )# if we want to crearte a single value inside the touple we should mention comma in the last (, )
print(tuble2)

print(tuble[2])# it print the indexing